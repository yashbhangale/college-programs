#include<stdio.h>
#include<conio.h>
void main()
{	int n,i,flag=1;
	clrscr();
	printf("\n\nEnter integer number :\n");
	scanf("%d",&n);
	for(i=2;i<n;++i)
	{
	if(n%i==0) {flag=0;break;}
	}
	if(flag==1) printf("Prime Number : %d",n);
	else printf("Not a Prime Number : %d",n);
	getch();
}
